[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "python-more-collections"
version = "0.1.0"
description = "OrderedSet and CaseInsensitiveDict collections"
readme = "README.md"
license = { text = "LGPL-3.0-or-later" }
requires-python = ">=3.8"
authors = [{ name = "bodencrouch" }]
dependencies = []

[project.urls]
Homepage = "https://github.com/bodencrouch/python-more-collections"
Repository = "https://github.com/bodencrouch/python-more-collections"

[tool.setuptools.packages.find]
where = ["src"]
